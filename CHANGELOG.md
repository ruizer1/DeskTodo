# 更新日志

本项目的版本号遵循 `主版本.次版本.修订号`。

## [1.0.0] — 2026-08-24

首个公开版本。

### 待办小组件

- 通过 Todoist 官方 API 双向同步，**不需要任何自建服务器**
- 显示周期可切换：1 天 / 3 天 / 1 周 / 1 个月，跨多天时按日期分组
- 自然语言快速添加（日期、标签、优先级由 Todoist 解析）
- 子任务进度显示与勾选、标签、优先级配色
- **离线可操作**：勾选和删除进本地队列，联网后自动补发
- 增量同步 + 自适应退避，面板收起时暂停同步

### 桌面图标整理框

- 桌面快捷方式按软件用途自动分成 6 类
- 依据目标路径、公司名、程序描述判断，而非简单按文件名
- 右键可改分类，改动永久记住
- 分类可折叠；无卡片背景，图标视觉上仍在桌面
- 可选：显示时自动隐藏真实桌面图标，退出时恢复

### 外观与窗口

- 3 套配色：蓝莓 / 薰衣草 / 暗色
- 两个面板各自独立的无级透明度
- 界面整体等比缩放（0.8×–1.8×）
- 三档窗口层级：桌面层 / 普通 / 置顶
- 记住位置尺寸、托盘常驻、可开机自启

### 安装

- 单文件安装包，内含 .NET 8 桌面运行时
- 安装时自动检测本机运行时，已有则不重复安装
