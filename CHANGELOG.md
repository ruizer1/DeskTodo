# 更新日志

本项目的版本号遵循 `主版本.次版本.修订号`。

## [1.0.2] — 2026-08-26

### 逾期任务不再消失（重要）

- **过期未完成的任务之前完全不显示**。日子一过，没做完的事就从面板上无声消失了 ——
  对一个待办工具来说这是最不该有的问题。现在区间包含今天时，
  列表最上方会固定出现一个红色的「逾期 · N 项未完成」分组，按到期日排序
- 翻到不包含今天的日期区间时，逾期分组不出现

### 新增：已完成任务的显示开关

- 设置 → **待办显示 → 继续显示已完成的任务**（默认开启）
- 勾上时已完成的任务会划掉留在列表里，方便回看今天做了什么；
  **过了任务自己的到期时刻会自动消失**，不会越堆越多。全天任务算到当天结束
- 关掉则已完成的任务立即不再显示。逾期未完成的任务不受这个开关影响

### 修复

- **默认显示的是明天而不是今天**。新安装打开后，今天到期的任务不在显示范围内，
  看起来就像没有同步成功
- **跨零点不再停在昨天**。程序挂一整天或一整周时，第二天面板上的日期区间
  和「今天 / 明天」这些标签会一起自动翻页
- **安装包不再包含本机源码路径**。调试符号文件里编进了开发机的完整目录结构，
  现已改为相对路径（崩溃日志的行号不受影响）

## [1.0.1] — 2026-08-25

### 覆盖安装

- 直接安装新版本即可完成更新，**不需要先卸载**。装完打开就能用：
  设置、配色、窗口位置、API 口令和本地任务缓存全部保留
- 更新时如果程序正在后台运行，安装程序会先请它退出，不再出现「文件被占用」
- 升级时桌面快捷方式和开机自启按现状勾选，不会因为升级把它们弄丢
- 同一时间只允许运行一个实例（之前重复启动会开出两个托盘图标和两套面板）

### 修复

- **服务端临时故障不再弹错误提示**。Todoist 偶尔返回 502 / 503，
  这类和断网一样属于「等会儿再来」，现在静默重试，不打扰使用
- **错误提示会自动消失**。之前后台同步失败留下的红字，
  即使下一次同步成功也不会清除，会一直挂在界面上
- **错误提示不再盖住列表内容**，改为独立一行，没有错误时不占空间
- **修复内存持续增长**。图标框重新扫描桌面时，解析快捷方式用的 COM 对象
  没有释放，长时间挂后台内存会一路上涨
- **关机不再被阻止**。之前关机或注销时可能弹出「此应用阻止关机」

### 改进

- 服务端持续故障时逐步拉长重试间隔（最长 10 分钟），
  并遵守服务端返回的 `Retry-After`，不再固定每分钟硬敲
- 桌面图标整理框改为默认开启

## [1.0.0] — 2026-08-24

首个公开版本。

### 待办小组件

- 通过 Todoist 官方 API 双向同步，**不需要任何自建服务器**
- 显示周期可切换：1 天 / 3 天 / 1 周 / 1 个月，跨多天时按日期分组
- 自然语言快速添加（日期、标签、优先级由 Todoist 解析）
- 子任务进度显示与勾选、标签、优先级配色
- **离线可操作**：勾选和删除进本地队列，联网后自动补发
- 增量同步 + 自适应退避，面板收起时暂停同步

### 桌面图标整理框

- 桌面快捷方式按软件用途自动分成 6 类
- 依据目标路径、公司名、程序描述判断，而非简单按文件名
- 右键可改分类，改动永久记住
- 分类可折叠；无卡片背景，图标视觉上仍在桌面
- 可选：显示时自动隐藏真实桌面图标，退出时恢复

### 外观与窗口

- 3 套配色：蓝莓 / 薰衣草 / 暗色
- 两个面板各自独立的无级透明度
- 界面整体等比缩放（0.8×–1.8×）
- 三档窗口层级：桌面层 / 普通 / 置顶
- 记住位置尺寸、托盘常驻、可开机自启

### 安装

- 单文件安装包，内含 .NET 8 桌面运行时
- 安装时自动检测本机运行时，已有则不重复安装
